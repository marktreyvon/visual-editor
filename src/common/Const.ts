/**
 * 默认画布容器ID
 */
export const DEFAULT_CONTAINER_ID = 'canvas_container';

/**
 * 默认缩略图容器ID
 */
export const DEFAULT_MINI_CONTAINER_ID = 'mini_container';

/**
 * 默认组件列表容器ID
 */
export const DEFAULT_STENCIL_CONTAINER_ID = 'stencil_container';

/**
 * 默认视图容器ID
 */
export const DEFAULT_DISPLAY_CONTAINER_ID = 'view_container';

/**
 * 是否显示网格
 */
export const DEFAULT_SHOW_GRID = true;

/**
 * 网格大小
 */
export const DEFAULT_GRID_SIZE = 10;

/**
 * 是否自动调整画布大小
 */
export const DEFAULT_AUTO_RESIZE = true;

/**
 * 是否开启框选
 */
export const DEFAULT_SELECTION = true;
/**
 * 是否开启撤销重做
 */
export const DEFAULT_HISTORY = true;

/**
 * 是否开启鼠标滚轮缩放
 */
export const DEFAULT_ENABLE_MOUSE_WHEEL = true; 

/**
 * 是否开启鼠标平移画布
 */
export const DEFAULT_ENABLE_MOUSE_PAN = true;

/**
 * 默认缩放因子
 */
export const DEFAULT_ZOOM_FACTOR = 0.2;

/**
 * 节点是否可旋转
 */
export const DEFAULT_ENABLE_ROTATING = true;

/**
 * 节点每次旋转的角度
 */
export const DEFAULT_RATATING_GRID = 15;

/**
 * 节点是否可移动
 */
export const DEFAULT_NODE_MOVABLE = true;

/**
 * 节点是否可调整大小
 */
export const DEFAULT_NODE_RESIZABLE = true;

/**
 * 预览大屏数据存储key
 */
export const PREVIEW_JSON_DATA_KEY = 'preview_json_data';

/**
 * 画布背景颜色
 */
export const DEFAULT_BACKGROUND_COLOR = '#F2F7FA';

